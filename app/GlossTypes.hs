-- GlossTypes has types based on Graphics.Gloss (mostly Color).
module GlossTypes (
  -- re-exports.
  Color
  , makeColor
  , rgbaOfColor
  , white
  , black
) where

import Graphics.Gloss (Color, makeColor, black, white, rgbaOfColor)
